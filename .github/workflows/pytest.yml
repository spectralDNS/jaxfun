name: Test jaxfun

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  build:
    name: python
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Run fast tests (default, excludes slow)
        run: |
          uv run pytest -m 'not slow'
          uv run pytest --float64 -m 'not slow'

      - name: Run slow demo tests
        run: |
          uv run pytest -m slow tests/test_demos.py
          uv run pytest --float64 -m slow tests/test_demos.py
